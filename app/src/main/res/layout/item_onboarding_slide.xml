<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <!-- Upper Card Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="310dp"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_onboarding_card"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Center Primary Circular Emblem -->
        <FrameLayout
            android:layout_width="104dp"
            android:layout_height="104dp"
            android:background="@drawable/bg_onboarding_circle">

            <ImageView
                android:id="@+id/iv_main_icon"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:layout_gravity="center"
                android:contentDescription="@null"
                android:tint="#FFFFFF"
                tools:src="@drawable/ic_onboarding_shield" />
        </FrameLayout>

        <!-- Floating Badges Row -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:gravity="center"
            android:orientation="horizontal">

            <FrameLayout
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginHorizontal="6dp"
                android:background="@drawable/bg_onboarding_badge">

                <ImageView
                    android:id="@+id/iv_badge_1"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:contentDescription="@null"
                    android:tint="@color/primary"
                    tools:src="@drawable/ic_onboarding_check" />
            </FrameLayout>

            <FrameLayout
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginHorizontal="6dp"
                android:background="@drawable/bg_onboarding_badge">

                <ImageView
                    android:id="@+id/iv_badge_2"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:contentDescription="@null"
                    android:tint="@color/primary"
                    tools:src="@drawable/ic_onboarding_lock" />
            </FrameLayout>

            <FrameLayout
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginHorizontal="6dp"
                android:background="@drawable/bg_onboarding_badge">

                <ImageView
                    android:id="@+id/iv_badge_3"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:contentDescription="@null"
                    android:tint="@color/primary"
                    tools:src="@drawable/ic_bell" />
            </FrameLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Slide Headline -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="28dp"
        android:gravity="center"
        android:textColor="?attr/colorOnSurface"
        android:textSize="22sp"
        android:textStyle="bold"
        tools:text="100% Free &amp; No Ads" />

    <!-- Slide Description -->
    <TextView
        android:id="@+id/tv_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="32dp"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.2"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="14sp"
        tools:text="Study without interruptions or paywalls." />

</LinearLayout>
